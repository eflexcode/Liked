<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eflexsoft.liked">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditProfileActivity"  android:theme="@style/withTheme" />
        <activity android:name=".MessageActivity" />
        <activity
            android:name=".LoginNumActivity"
            android:theme="@style/withTheme" />
        <activity
            android:name=".CreateAccountGoogle"
            android:theme="@style/withTheme" />
        <activity
            android:name=".CreateAccountPhoneNumberActivity"
            android:theme="@style/withTheme" />
        <activity
            android:name=".CreateAccountEmailPasswordActivity"
            android:theme="@style/loginTheme" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/loginTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/mainAcTheme" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/splashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>